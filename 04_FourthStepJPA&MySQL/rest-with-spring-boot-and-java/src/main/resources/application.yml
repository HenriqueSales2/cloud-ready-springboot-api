spring:
  application:
    name: rest-with-spring-boot-and-java
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot_java?userTimezone=true&serverTimezone=UTC
    username: root
    password: Plastation19112007
  jpa:
    hibernate:
      ddl-auto: update # através desse comando o banco de dados sabe que precisa criar a tabela para a aplicação
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show_sql: false # ativar essa funcao somente para debugar